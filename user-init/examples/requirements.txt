# Example user-installable packages for BrightSign Python extension
# Place this file in /storage/sd/python-init/requirements.txt
#
# IMPORTANT: Do NOT list packages already in the SDK
# Pre-installed packages include:
#   - Python 3.8 standard library
#   - numpy, scipy, pandas
#   - opencv (cv2), pillow (PIL), scikit-image
#   - torch, torchvision
#   - rknnlite (NPU acceleration)
#   - matplotlib, seaborn
#   - Common utilities: protobuf, flatbuffers, psutil, tqdm, etc.
#
# Only add packages you need that are NOT pre-installed in the SDK

# Example: Extended OpenCV modules (extra algorithms not in standard build)
#opencv-contrib-python==4.10.0.84

# Example: Additional ML/AI libraries
#scikit-learn
#xgboost

# Example: Application utilities
#APScheduler==3.10.1
#requests==2.32.4
#redis==5.2.1
#PyYAML==6.0.2
#python-dateutil==2.8.2

# Example: Data processing
#openpyxl
#xlrd

# Example: Communication protocols
#pyserial==3.4
#modbus-tk

# Note: User-init system automatically applies --only-binary=:all: flag
# This prevents compilation on the player (which has no build tools)
